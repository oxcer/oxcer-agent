[package]
name = "oxcer-core"
version = "0.1.0"
edition = "2021"
description = "Core logic for Oxcer: FS Service, Shell Service, Security Policy, Logging"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs-next = "2"
mime_guess = "2"

[dev-dependencies]
tempfile = "3"
