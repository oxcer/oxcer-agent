# Oxcer
Local Agentic AI with Shields

**Development:** Core logic lives in the `oxcer-core` library (no Tauri); test with `cargo test -p oxcer-core`. The Tauri app in `src-tauri` is a thin launcher; run it with `cargo tauri dev`. See [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) for workflow.
